[project]
name = "py-test-uv-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "plotly-express>=0.4.1",
    "polars[pyarrow]>=1.34.0",
    "pyarrow>=22.0.0",
]
